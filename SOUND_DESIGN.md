# Саунд-дизайн — Match-3 Boss Fight

Мобильная казуалка, бой с аниме-боссом. Яркие, сочные звуки.

**Форматы**: OGG + MP3 | **Папка**: `assets/sfx/` | **Громкость**: нормализация -6 dB

---

## 1. Поле — тайлы и матчи (7 звуков)

| Файл | Что происходит | Хронометраж |
|------|---------------|-------------|
| `sfx_swap` | Игрок свайпнул — два тайла поменялись местами | 100–150 мс |
| `sfx_swap_fail` | Свап не дал матча — тайлы вернулись обратно. Также: попытка двинуть бомбу | 150–200 мс |
| `sfx_match` | Три+ тайла совпали и улетают с поля | 200–300 мс |
| `sfx_cascade` | Цепная реакция — новые тайлы упали и снова совпали (x2, x3…) | 200–300 мс |
| `sfx_special_create` | Матч 4+ — обычный тайл превратился в усиленный (золотое/красное свечение) | 200–400 мс |
| `sfx_special_activate` | Тап по усиленному тайлу — он взрывается, очищая область | 300–500 мс |
| `sfx_tile_collapse` | Тайлы падают вниз, заполняя пустые ячейки | 100–200 мс |

---

## 2. Бой — урон и ресурсы (5 звуков)

| Файл | Что происходит | Хронометраж |
|------|---------------|-------------|
| `sfx_boss_hit` | Тайлы мечей/звёзд долетели до босса — босс получает урон, трясётся | 200–400 мс |
| `sfx_player_hit` | Игрок получает урон (от атаки босса или взрыва бомб) | 200–400 мс |
| `sfx_shield_block` | Игрок атакует, но щит босса поглощает весь урон — «дзынь» | 200–300 мс |
| `sfx_heal` | Тайлы лечения долетели до аватара — HP восстанавливается | 200–400 мс |
| `sfx_mana` | Тайлы маны долетели до аватара — полоска маны растёт | 200–300 мс |

---

## 3. Скиллы игрока (3 звука)

| Файл | Что происходит | Хронометраж |
|------|---------------|-------------|
| `sfx_skill` | Игрок нажал кнопку скилла (удар / стан / лечение) — эффект применяется | 300–500 мс |
| `sfx_hammer` | Молот — игрок убирает один конкретный тайл с поля | 200–400 мс |
| `sfx_skill_unlock` | **НОВЫЙ.** Каждые 3 хода открывается новый скилл — оверлей + пульсация кнопки | 500–800 мс |

---

## 4. Способности босса (7 звуков)

| Файл | Что происходит | Хронометраж |
|------|---------------|-------------|
| `sfx_ability_announce` | Экран затемняется, полноэкранная картинка босса + название способности | 300–500 мс |
| `sfx_boss_attack` | Босс бьёт игрока — обычная атака и мощный удар, камера трясётся | 300–500 мс |
| `sfx_shield_activate` | Босс включает щит — блокирует весь урон на 2 хода | 300–500 мс |
| `sfx_shield_break` | **НОВЫЙ.** Щит истёк — исчезает с затуханием | 200–400 мс |
| `sfx_mana_drain` | Мощный удар босса сливает 50 маны у игрока | 300–500 мс |
| `sfx_bomb_place` | Босс ставит 5 бомб на поле — тайлы превращаются в бомбы с обратным отсчётом | 300–500 мс |
| `sfx_bomb_explode` | Таймер бомбы дошёл до 0 — взрыв, урон игроку | 300–500 мс |
| `sfx_bomb_defuse` | Игрок собрал матч рядом с бомбой — бомба обезврежена (зелёная вспышка) | 200–400 мс |

---

## 5. Интро-сцена (4 звука)

| Файл | Что происходит | Хронометраж |
|------|---------------|-------------|
| `sfx_boss_reveal` | **НОВЫЙ.** Босс-девочка появляется из затемнения — драматичное появление | 500–1000 мс |
| `sfx_dialogue_pop` | **НОВЫЙ.** Всплывает облачко с репликой (босс или игрок говорит) | 100–200 мс |
| `sfx_vs` | Экран «VS» — аватары игрока и босса друг напротив друга | 500–1000 мс |
| `sfx_intro_zoom` | **НОВЫЙ.** Камера зумится на фон перед VS экраном | 300–600 мс |

---

## 6. Конец игры (2 звука)

| Файл | Что происходит | Хронометраж |
|------|---------------|-------------|
| `sfx_victory` | HP босса = 0 — экран победы, жёлтая вспышка | 1–2 сек |
| `sfx_defeat` | HP игрока = 0 — экран поражения, красная вспышка | 1–2 сек |

---

## 7. UI и вспомогательные (5 звуков)

| Файл | Что происходит | Хронометраж |
|------|---------------|-------------|
| `sfx_reshuffle` | **НОВЫЙ.** Нет доступных ходов — все тайлы перемешиваются, надпись «Перемешиваю!» | 400–700 мс |
| `sfx_hint` | **НОВЫЙ.** Игрок бездействует 3+ сек — тайлы-подсказки покачиваются и светятся | 200–400 мс |
| `sfx_ui_click` | **НОВЫЙ.** Клик по кнопке (Restart, Retry, настройки) | 50–100 мс |
| `sfx_ui_error` | **НОВЫЙ.** Клик по заблокированному скиллу / недоступное действие | 100–200 мс |
| `sfx_whoosh` | **НОВЫЙ.** Босс меняет позу — dissolve-переход спрайта в атакующую стойку | 200–400 мс |

---

## Итого: 36 звуков

- Готовые (нужно заменить): **24**
- Новые (нужно создать): **12** — помечены **НОВЫЙ**
